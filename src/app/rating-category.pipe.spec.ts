import { RatingCategoryPipe } from './rating-category.pipe';

describe('RatingCategoryPipe', () => {
  it('create an instance', () => {
    const pipe = new RatingCategoryPipe();
    expect(pipe).toBeTruthy();
  });
});
